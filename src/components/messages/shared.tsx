import styled from "styled-components";

import { SENDER_COLOR } from "constants/colors";

export const Sender = styled.h5`
  display: inline;
  font-size: 1rem;
  font-weight: bold;
  margin: 0;
  color: ${SENDER_COLOR};
`;
